 <table cellpadding="0" cellspacing="0" border="0" class="dynamicTable display table table-bordered" width="100%">
                                        <thead>
                                            <tr>
                                               <th>Statut</th>
                                               {% for instance in instances %}
                                                <th>{{ instance }}</th>
                                               {% endfor %}
                                            </tr>
                                        </thead>
                                        <tbody>
                                         {% for states in status %} 
                                             {% for cle,ligne in tableau %} 
                                                 <tr class="even gradeC">
                                                   <td>{{ cle }}</td>
 									     				{% for val in ligne %}
	 									     				     <td>{{ val }}</td>
 									     				{% endfor %} 
                                                </tr>
                                              {% endfor %}
                                          {% endfor %}   
                                        </tbody>
                                    </table>