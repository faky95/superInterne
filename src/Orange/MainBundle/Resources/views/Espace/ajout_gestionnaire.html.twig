<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal">
		<span class="icon12 minia-icon-close"></span>
	</button>
	<h4 align="left">
		<span class="icon16 icomoon-icon-earth"></span>
       	<span>Ajouter un gestionnaire</span>
	</h4>
</div>
<form class="form-horizontal" action="{{ path('ajout_gestionnaire', { 'id': entity.id, 'user_id': user.utilisateur.id }) }}" method="POST" novalidate="novalidate" role="form">
	<div class="panel-body">
		{% if(user.isGestionnaire==0) %}
		<p>
		Voulez-vous vraiment définir ce membre comme gestionnaire?
		 </p>
		  {% else %}
		<p>
		Voulez-vous vraiment enlever ce membre des gestionnaires?
		 </p>
		{% endif %}
		<!-- End .form-group  -->
	</div>
	<div class="form-actions">
		<div class="col-lg-offset-2 col-lg-9">
			<a data-target="#myModal" class="btn btn-success submitLink" call="redirectModalToUrl">Confirmer</a>
			<a class="btn btn-primary" data-dismiss="modal">Fermer</a>
		</div>
	</div>
</form>