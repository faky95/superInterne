		<div class="modal-header">
	<button type="button" class="close" data-dismiss="modal">
		<span class="icon12 minia-icon-close"></span>
	</button>
	<h4 align="left">
		<span class="icon16 icomoon-icon-earth"></span>
       	<span>Ajout avancement</span>
	</h4>
</div>
 <form novalidate="novalidate" class="form-horizontal" action="{{ path('actionavancement_create', {'action_id': action_id}) }}" method="post" role="form">
							{{ form_row(form.description,  {'attr': {'class': 'nostyle form-control'}}) }}
			                {{ form_row(form._token) }}
					             <div class="form-group form-actions">
							<div class="col-lg-offset-3 col-lg-12">
							    {{ form_widget(form.save,{attr: {'data-target': '#myModal', 'call': 'redirectModalToUrl', 'class':'btn btn-warning submitLink'}}) }}
							</div>
						</div><!-- End .form-group  -->  
</form>