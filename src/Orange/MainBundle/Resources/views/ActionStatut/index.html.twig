{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ActionStatut list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datestatut</th>
                <th>Commentaire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('actionstatut_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateStatut %}{{ entity.dateStatut|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.commentaire }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('actionstatut_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('actionstatut_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('actionstatut_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
