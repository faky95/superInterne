{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SignalisationAnimateur</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dateaffectation</th>
                <td>{{ entity.dateAffectation|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{{ entity.actif }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('signalisationanimateur') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('signalisationanimateur_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
