{% extends '::base.html.twig' %}


{% block content -%}
	<!-- heading -->
        <div class="heading">
                <h3>{% block page_title %} Liste des formules {% endblock %}</h3>                    
        </div>
        <!-- End .heading-->
	<div class="row">
                        <div class="col-lg-12">
                        <div class="row">
							<div class="col-lg-4">
								 <a href="{{ path('nouvelle_formule') }}"><button class="btn btn-warning"><span class="icon16 silk-icon-plus white"></span> Ajouter d'une formule</button></a> 
								 <div class="marginB10"></div>
							</div><!-- End .span4 -->
						</div>
                            <div class="panel panel-default gradient">
                                <div class="panel-heading">
                                    <h4>
                                        <span class="icon16 brocco-icon-grid"></span>
                                        <span>Liste des formules</span>
                                    </h4>
                                </div>
                                <div class="panel-body noPad clearfix">
                                    <table id="formuleTable" cellpadding="0" cellspacing="0" border="0" class=" display table table-bordered" width="100%">
                                        <thead>
                                            <tr>
                                            	<th  width="40px">Couleur</th>
                                                <th>Libellé</th>
                                                <th width="150px">Visible à l'accueil</th>
											    <th width="120px">Actions</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                        <tr>
                                        	<td colspan="4">chargement des donnees</td>
                                        </tr>
                                        </tbody>
                                    </table>
                            </div><!-- End .panel -->

                        </div><!-- End .span12 -->

					</div><!-- End .row --> 
	
	
	
	
    
{% endblock %}
{% block pagescript %}
	{{ parent() }}
	{{ block_render('::form_div_layout.html.twig','datatable_options',{with_tag: 1}) }}
	{% include 'OrangeQuickMakingBundle:Extra:datatable.html.twig' with {idTable: 'formuleTable', url: path('liste_des_formules'), nosorting: {0: 2}} %}
{% endblock %}