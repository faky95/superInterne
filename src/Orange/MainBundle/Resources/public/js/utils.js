
/**
 * Cette fonction permet de  faire une requette ajax pour charger les données d'un div
 * @param object : recuperer la valeur selectionné dans le select 
 * @param div : la div a changer 
 * @param url : url du controler a appeler
 */
function alertAfterModalError() {
	
}

function formModalSubmit() { 
}

function todoOnCompleteWithError() {
	$('input[type="file"]').uniform();
}

function select2() {
}
